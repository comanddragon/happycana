commit 5819f98e91f392c2271b1f0048d2a81d8b2abf9b
Author: Claude <claude@anthropic.com>
Commit: Claude <claude@anthropic.com>

    sitemap.ts: replace locally-caught throw with a direct return
    
    IntelliJ flagged 'throw of exception caught locally' on the product
    pagination loop's throw new Error(...) — it's thrown and caught within
    the same try/catch in this one function, so it was just an indirect
    way of returning early. Return the fallback (static + blog entries)
    directly instead. The other throw in getAllBrandsForSitemap() is left
    as-is since it legitimately crosses a function boundary into this
    function's catch block.
