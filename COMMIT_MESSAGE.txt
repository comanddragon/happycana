Restyle google-pay.svg to match the badge+wordmark pattern (like venmo.svg/apple-pay.svg)

The provided icon paths had the 'Pay' wordmark baked into the badge
glyph alongside the G mark, which duplicated the outer text label and
overflowed the badge bounds. Keeps only the four-color G logo, scales
and centers it in the 36x36 badge the same way apple-pay.svg's glyph
is centered, and matches its wordmark font-size (23) and title casing
('Google Pay') for visual consistency across the two.

