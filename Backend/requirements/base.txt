# =============================================================================
# requirements/base.txt
# Packages required in every environment
# =============================================================================

# Core
Django==6.0.3
python-dotenv==1.2.2
psycopg2-binary==2.9.11        # PostgreSQL adapter
django-ratelimit==4.1.0

# REST API
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-tasks==0.12.0
django-filter==25.2
drf-spectacular==0.29.0        # OpenAPI / Swagger schema generation

# Realtime
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.1                 # ASGI server

# Cache & storage
redis==6.4.0
django-storages==1.14.6        # S3 / GCS backends
boto3==1.42.61                 # AWS SDK (S3, SES, SQS)

# Static files
whitenoise==6.12.0

# Utilities
Pillow==12.1.1                 # Image processing
pillow-avif-plugin==1.6.0      # AVIF read/write support for Pillow

stripe==14.4.0
resend==2.42.0
requests==2.32.5
