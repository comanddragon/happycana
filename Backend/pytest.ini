[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py tests.py
addopts = --strict-markers
markers =
    live_email: sends a real email through the configured provider (opt-in only)
