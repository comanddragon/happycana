{% extends "emails/base_email.txt" %}

{% block content %}
Order received

Thanks{% if order.user.first_name %}, {{ order.user.first_name }}{% endif %} — we've received order #{{ order.id }}, placed {{ order.created_at|date:"M j, Y" }}.
Here's a copy for your records. We'll follow up separately to arrange payment, and you'll get another email once that's confirmed.

Items:
{% for item in items %}- {{ item.variant.product.name }} ({{ item.variant.sku }}) x{{ item.quantity }} — ${{ item.total_price }}
{% endfor %}
Subtotal: ${{ order.subtotal }}
{% if order.discount_amount %}Discount: -${{ order.discount_amount }}
{% endif %}Shipping: ${{ order.shipping_cost }}
Total: ${{ order.total }}

Payment method: {{ order.payment_method.name }}
{{ order.payment_method.description }}

Shipping to:
{{ order.address.line1 }}{% if order.address.line2 %}, {{ order.address.line2 }}{% endif %}
{{ order.address.city }}, {{ order.address.state }} {{ order.address.postal_code }}
{{ order.address.country }}

View order: {{ store_url }}/orders/{{ order.id }}
{% endblock %}
